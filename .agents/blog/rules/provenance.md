# Provenance

`references` is reserved for internal Markdown relationships. It is not a bibliography. The generated file must name the source Markdown file exactly as provided and must not claim a source revision different from the supplied source commit.

Generated content is always a draft. A human reviews the pull request before it can be merged or published.
